<Window x:Class="Task_3_Reflector_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_3_Reflector_"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Відкрити збірку..." Click="OpenMenuItem_Click"/>
                <Separator />
                <MenuItem Header="Вихід" Click="CloseMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <Grid Background="#F5F5F7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="10" VerticalAlignment="Top">
                <TextBlock Text="Фільтр відображення:" FontWeight="Bold" Margin="0,0,0,10" FontSize="14"/>

                <CheckBox Name="ShowFieldsCheckBox" Content="Поля" IsChecked="True" Margin="0,5"/>
                <CheckBox Name="ShowPropertiesCheckBox" Content="Властивості" IsChecked="True" Margin="0,5"/>
                <CheckBox Name="ShowMethodsCheckBox" Content="Методи" IsChecked="True" Margin="0,5"/>

                <Separator Margin="0,10"/>

                <CheckBox Name="ShowAttributesCheckBox" Content="Показувати атрибути" IsChecked="True" Margin="0,5" FontWeight="Medium"/>
            </StackPanel>

            <TextBox Name="ResultTextBox" 
                     Grid.Column="1"
                     Margin="10" 
                     FontFamily="Consolas" 
                     FontSize="13"
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto" 
                     IsReadOnly="True" 
                     AcceptsReturn="True" />
        </Grid>
    </DockPanel>
</Window>
